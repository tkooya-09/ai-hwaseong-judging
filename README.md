# AI화성 챌린지 심사집계표

## 현장판 1.1 변경사항

- 심사위원 화면: **내 심사표 출력**. 운영자 화면: 위원별 **심사표 출력**. 6개 작품의 5개 항목, 작품별 합계, 개인 서명란을 출력합니다. 미제출 자료는 확인용이라고 표시합니다.
- 최종 결과: 기존 **결과·서명부 출력 (A4 1장)** 유지. 시민·공무원 결과 아래 5명 자필 서명란이 나옵니다.
- 미저장 입력을 기기에 임시 보관합니다. 새로고침 후 같은 위원으로 접속하면 서버 버전이 같을 때만 복구합니다. 다른 기기에서 수정됐거나 이미 제출된 경우 자동 덮어쓰기 없이 **복구 백업 다운로드**로 확인할 수 있습니다.
- 통신이 복구되면 미저장 입력을 재전송합니다. 충돌이 있으면 백업 후 최신 점수를 확인해야 합니다.
- 다른 심사위원의 저장으로 현재 입력란의 초점이 사라지지 않도록 갱신 동작을 개선했습니다.

## 9월 17일 현장 운영 순서

1. 09:30: 운영자 ‘전명구’와 심사위원 본인 이름으로 각 기기에서 접속합니다. 상단의 ‘공동 집계 연결’을 확인합니다.
2. 09:40: 위원 이름 5명, 작품 6개, 평가항목 5개(각 20점)를 확인하고 작품 링크를 열어 봅니다. **단일 PC 모드는 공동 집계되지 않으므로 현장 다중 기기 심사에는 사용하지 않습니다.**
3. 10:00: 심사를 시작합니다. 각 위원은 6작품의 30개 항목을 입력하고 ‘저장됨’을 확인합니다.
4. 각 위원이 ‘내 심사 완료 · 제출’을 누릅니다. 운영자는 5명 제출을 확인하고 ‘전체 심사 완료 · 결과 집계’를 누릅니다.
5. 결과·서명부를 A4 가로, 배율 100%, 머리글/바닥글 해제로 인쇄하고 결과 아래 5명 서명을 받습니다. 개별 심사표도 별도로 출력할 수 있습니다.
6. 최종 CSV와 전체 점수 JSON을 내려받아 보관합니다. 동점은 공동순위로 표시되며 수상등급은 심사위원 협의 후 확정합니다.
7. 정정이 필요하면 운영자가 사유를 입력해 해당 위원의 수정을 허용합니다. 해당 위원이 재제출하고 전체 집계를 다시 완료한 다음 정정된 결과를 출력합니다.

### 통신 장애와 복구

‘저장 실패’이면 서버에 저장되었다고 간주하지 않습니다. 현재 입력은 화면과 기기 임시저장에 유지되며, **내 입력 백업**도 내려받을 수 있습니다. 연결 복구 후 ‘지금 저장’을 누르고 운영자 화면에서 반영을 확인합니다. 다른 화면과 버전 충돌이 나면 먼저 복구 백업을 내려받고 ‘최신 점수 불러오기’로 서버 점수를 확인한 뒤 필요한 항목을 다시 입력합니다. 임시저장은 해당 기기·브라우저 기준이며 기기 고장이나 브라우저 데이터 삭제에 대비한 서버 백업을 대신하지 않습니다.

자동 검증: 12개 통과(5명 동시 저장 모사, 권한별 응답, 제출 잠금, 미입력·0점·범위 검사, 순위·동점, 버전 충돌, 새로고침 복구, 실패 후 재시도, 저장 중 추가입력, 개별 출력). 운영 점수에 테스트 값을 쓰지 않았습니다.

## 간편 접속

심사위원은 첫 화면에서 본인 이름을 정확히 입력합니다. 등록된 5명만 접속되며 본인 역할의 심사표가 열립니다. 운영자는 이름 입력칸에 전명구를 입력합니다. 운영자 접속 버튼에서도 이름으로 접속할 수 있습니다.

이름 접속은 본인 인증이 아닙니다. 다른 사람의 이름을 아는 사람도 그 위원으로 접속할 수 있습니다. 행사 참여자에게만 주소를 공유하세요. 전명구라는 이름으로 운영자 권한에 접근할 수 있으므로 이름 자체가 비밀번호 역할을 하지 않습니다.

Vercel 로그인 보호가 켜져 있으면 이름 화면보다 먼저 Vercel 로그인이 표시됩니다. 이 설정은 Vercel 프로젝트에서 별도로 해제해야 합니다.


2026. 9. 17. 시민·공무원 AI 공모전 최종 발표심사를 위한 HTML 심사집계 화면입니다.
HTML만으로 한 PC에서 사용할 수 있고, 동봉한 API와 데이터 저장소를 연결하면 여러 기기의 공동 집계가 가능합니다. 공동 집계 저장소는 Supabase Postgres이며, Edge Function이 접속 인증과 점수 저장을 담당합니다.

## 1. HTML을 바로 사용하는 방법

1. `dist/index.html`을 Chrome 또는 Edge에서 엽니다. 별도 설치나 인터넷 연결 없이 점수 입력·집계 기능을 사용할 수 있습니다. 작품 사이트를 열 때는 인터넷 연결이 필요합니다.
2. 처음에는 운영자 화면이 표시됩니다. 왼쪽 ‘입력 담당’에서 심사위원을 선택합니다.
3. 작품을 선택하고, ‘작품 사이트 열기’로 시연을 확인한 다음 5개 항목의 점수를 입력합니다.
4. 점수는 입력을 멈춘 뒤 약 0.65초 후 자동 저장됩니다. ‘저장됨’을 확인합니다. 빈칸은 미입력이며 0점과 다르게 처리합니다.
5. 심사위원 1명당 6개 작품, 총 30개 항목을 입력하고 ‘내 심사 완료 · 제출’을 누릅니다.
6. 다른 심사위원으로 바꿔 같은 과정을 진행합니다. 모두 제출한 뒤 ‘운영자’를 선택합니다.
7. ‘전체 심사 완료 · 결과 집계’를 누르면 시민·공무원 부문별 합계·평균·순위가 표시됩니다.
8. ‘CSV 다운로드’로 결과와 항목별 세부점수를 내려받고, ‘전체 점수 백업’으로 기록을 보관할 수 있습니다.

**단일 PC 모드는 같은 브라우저의 한 탭에서 사용하세요.** 심사위원 선택 기능은 한 PC 입력용이며 접속 인증 기능이 아닙니다. 브라우저 저장소 삭제, 다른 브라우저 사용, HTML 파일 경로 변경 등으로 기존 점수를 읽지 못할 수 있습니다. 결과를 CSV·JSON으로 보관하세요. 여러 기기의 점수가 자동으로 합쳐지지는 않습니다. 업데이트한 파일에서 기존 점수가 보이지 않으면 기존 파일에서 저장한 전체 점수 백업 JSON을 ‘점수 백업 불러오기’로 불러올 수 있습니다. 이 기능은 단일 PC 모드의 운영자 화면에서만 제공하며, 공동 집계 서버의 데이터를 변경하지 않습니다.

## 결과표·서명부 한 장 출력

1. 운영자 화면에서 전체 심사를 완료하고 **최종 집계**로 이동합니다.
2. **결과·서명부 출력 (A4 1장)** 버튼을 누릅니다.
3. 인쇄창에서 **A4 / 가로 / 배율 100% / 머리글 및 바닥글 해제**를 선택합니다. 문서 여백은 10mm로 지정되어 있습니다. 프린터 설정에서 여백을 별도로 크게 지정하면 페이지가 늘어날 수 있습니다.
4. 시민·공무원 결과표 2개와 위원 5명의 자필 서명란이 한 장에 출력됩니다. PDF로 보관하려면 인쇄 대상을 ‘PDF로 저장’으로 선택합니다.
5. 출력 후 **정희석·김규진·홍아름·서호성·정원석** 위원의 서명을 받고 확인일을 적습니다.

표에는 작품명·발표자·위원별 총점·500점 합계·100점 평균·부문별 순위·참고 수상등급이 포함됩니다. 서명란은 자필 서명을 받을 빈 공간이며 전자서명을 대신하지 않습니다. 화면에 현재 저장된 실제 집계결과를 사용하며, 예시 점수를 자동으로 입력하지 않습니다. 출력 버튼은 최신 저장 상태를 다시 확인하여 재심사 중인 결과가 출력되는 것을 차단합니다.

업데이트 방법: 기존 파일에서 **‘전체 점수 백업’으로 JSON을 먼저 저장**한 뒤, 새 HTML을 실행하세요. 기존 점수가 표시되지 않으면 운영자 화면의 **‘점수 백업 불러오기’**로 해당 JSON을 선택합니다. 불러오기는 기존 로컬 데이터를 교체하므로 확인창의 내용을 확인하세요. GitHub/Vercel 사용자는 ZIP 소스를 반영해 다시 배포하면 됩니다. 기존 `EVENT_ID`와 서버 환경변수는 유지합니다.

## 2. GitHub 등록 및 Vercel 배포 준비

### 파일 구성

| 파일 | 용도 |
|---|---|
| `dist/index.html` | 바로 실행 가능한 단일 HTML 완성본 |
| `index.template.html` | HTML 화면·스타일·동작 수정용 원본 |
| `lib/data.json` | 작품 6개, 심사위원 5명, 평가기준 원본 |
| `lib/core.cjs` | 점수 검증·합계·순위·권한·완료 처리 공통 로직 |
| `lib/print.cjs` | 결과표와 5명 서명란의 인쇄 전용 양식 |
| `api/state.js` | Vercel 공동 집계 API |
| `scripts/build.cjs` | 자료와 공통 로직을 단일 HTML로 결합 |
| `scripts/create-codes.cjs` | 운영자·심사위원별 접속 코드와 해시 생성 |
| `vercel.json` | Vercel 빌드·배포 설정 |
| `.env.example` | 필요한 서버 환경변수 양식 |
| `tests/` | 점수 로직과 API 검증 |

### Supabase 공동 집계 구성

- Vercel `/api/state` → Supabase `judging-state` Edge Function → Postgres.
- 운영자/심사위원 접속 코드는 SHA-256 해시만 DB에 저장합니다. 실제 코드는 소스와 HTML에 넣지 않습니다.
- `hwaseong_judging_events`: 행사별 심사 상태. `revision` 조건부 UPDATE로 동시 저장 충돌을 감지하고 재시도합니다.
- `hwaseong_judging_codes`: 코드 해시·역할·행사 ID. 코드에 연결된 행사만 접근하며 다른 위원 점수는 서버에서 가립니다.
- 두 테이블은 RLS가 켜져 있으며 anon/authenticated 권한은 없습니다. Supabase 서버만 접근합니다.
- Edge Function은 심사위원 명단 또는 운영자 이름 또는 기존 접속 코드를 확인하므로 `verify_jwt=false`로 배포합니다. 등록된 이름이나 유효한 코드가 없는 요청은 거절합니다.
- Supabase가 제공하는 서버 키를 Edge Function에서만 사용합니다. Vercel 환경변수에는 Redis 키나 접속 코드를 넣을 필요가 없습니다.
- 다른 기기의 저장 결과는 약 3초마다 조회됩니다. WebSocket 방식 Supabase Realtime 구독은 사용하지 않습니다.

### 다른 프로젝트에 설치할 때

1. `supabase/setup.sql`을 새 프로젝트에 한 번 적용합니다.
2. 행사 초기 상태를 `hwaseong_judging_events`에 넣고 접속 코드 해시를 `hwaseong_judging_codes`에 등록합니다. 실제 코드는 각 담당자에게만 전달합니다.
3. `node scripts/build-edge.cjs` 실행 후 `supabase/functions/judging-state`를 배포합니다.
4. `api/state.js`의 공개 Edge Function URL을 대상 프로젝트 URL로 바꾸고 Vercel에 재배포합니다.
5. 운영용 행사 데이터와 검증용 데이터는 별도 event_id 및 별도 코드로 분리합니다.

## 3. 심사 기준과 계산 방식

| 항목 | 배점 | 기준 |
|---|---:|---|
| 창의성 | 20 | 내용의 독창성 및 동일·유사 사례 여부 |
| 효율성 | 20 | 비용·인력 등 투입 대비 결과와 행정혁신 효과 |
| 적용범위 | 20 | 실제 적용 가능한 대상 및 수혜 대상의 범위 |
| 계속성 | 20 | AI 활용의 점진적 확대를 위한 연차별 준비사항 |
| 기술성 | 20 | 핵심 기술의 실질적 구현 정도 및 출품작의 완성도 |
| 합계 | 100 | 심사위원 1명 기준 |

- 항목별 0~20점 정수 입력입니다. 음수, 20점 초과, 소수는 차단합니다.
- 5명 점수를 모두 합산하여 작품별 500점 만점으로 계산합니다. 평균은 합계 ÷ 5, 100점 만점으로 소수 둘째 자리까지 표시합니다.
- 최고·최저점 제외, 가점, 항목별 추가 가중치는 적용하지 않습니다.
- 시민 3작품과 공무원 3작품을 각각 집계합니다. 6개 작품을 섞어 통합순위를 매기지 않습니다.
- 동점이면 공동순위로 표시합니다. 예를 들어 1위가 2작품이면 `1·1·3`입니다. 동점 작품의 수상등급은 ‘협의 필요’로 표시합니다.
- 점수 순위에 따른 대상·최우수상·우수상은 참고 표시입니다. 시상등급·인원 변경 등 최종 결정은 이 버전에서 자동 처리하지 않습니다.

## 4. 역할과 운영 절차

| 기능 | 심사위원 | 운영자 |
|---|---|---|
| 작품 소개·시연 사이트 열기 | 가능 | 가능 |
| 점수 입력 | 본인 점수만 | 공동 집계 모드에서는 불가 |
| 다른 위원의 항목별 점수 조회 | 불가 | 가능(API·백업), 화면은 현황 중심 |
| 심사표 제출 | 본인 심사표 | 불가 |
| 제출 완료 상태 확인 | 가능 | 가능 |
| 전체 완료·순위·CSV 출력 | 불가 | 가능 |
| 제출한 심사표 수정 허용 | 불가 | 사유 입력 후 가능 |

운영자는 ‘심사 현황’에서 제출 완료 위원의 ‘수정 허용’을 누르고 사유를 기록할 수 있습니다. 해당 심사표의 제출과 전체 집계 완료 상태가 해제됩니다. 위원이 점수를 수정해 다시 제출하면 운영자가 전체 완료를 다시 진행합니다. 변경 사유·시간·역할·버전은 전체 백업의 `audit`에 남습니다. 점수 입력 기록은 작업 이력이며, 모든 과거 점수 값의 버전 보관 기능은 아닙니다.

연결이 끊기면 화면에 실패가 표시되고 미저장 입력은 현재 열린 화면에서 유지됩니다. ‘지금 저장’으로 재시도할 수 있습니다. 저장에 실패한 상태에서 창을 닫기 전 ‘내 입력 백업’으로 내용을 저장하세요. 공동 집계 모드의 미저장 초안은 새로고침·창 닫기 후 자동 복구되지 않습니다.

같은 위원 코드로 여러 창에서 동시에 수정하면 버전 충돌을 감지합니다. ‘내 입력 백업’으로 기록을 남기고 ‘최신 점수 불러오기’를 누른 뒤 필요한 점수만 다시 입력합니다. 서로 다른 위원의 동시 저장은 Postgres 버전 조건부 갱신으로 병합되어 다른 위원 점수를 덮어쓰지 않습니다.

## 5. 반영 자료

- `2026년 공모전 발표심사 준비계획_v2.hwpx`: 참고1 평가기준·심사위원, 참고2 제안 현황·발표 순서.
- `시민, 공무원 심사표.xlsx`: 작품 설명과 작품 URL.
- 준비계획 첫 부분의 ‘2026.9.17.(화)’는 같은 문서 참고1의 ‘2026.9.17.(목)’와 맞춰 목요일로 반영했습니다.
- 준비계획 참고3 연락처 표는 시민·공무원 구분이 다른 표와 상충하여, 참고2와 엑셀의 일치하는 구분을 적용했습니다.
- 연락처·이메일은 HTML과 배포 자료에 포함하지 않았습니다. 실제 링크의 가동 여부는 확인하지 않았으며 첨부자료의 URL을 그대로 연결했습니다.

## 6. 수정 및 검증

작품·심사위원 표시를 수정하려면 `lib/data.json`, 화면을 수정하려면 `index.template.html`을 편집하고 다음 명령을 실행합니다. 출력된 `dist/index.html`은 다른 파일 없이 단독 실행됩니다. `dist/index.html`만 수정하면 다음 빌드 때 덮어써지므로 수정용 원본을 편집하세요.

```bash
npm run build
npm test
```

현재 집계 로직은 이번 행사에 맞춰 **작품 6개·위원 5명·항목 5개·항목별 20점**을 전제로 합니다. 개수·배점 변경은 `lib/core.cjs`, HTML의 진행률·입력범위, 검증코드를 함께 수정해야 합니다. 심사 도중 ID나 부문을 변경하지 말고 별도 행사 공간에서 먼저 확인하세요.

검증 결과: 8개 자동 검증 통과. 점수 범위·미입력·0점·합계·평균·부문별 순위·동점·제출 잠금·수정 허용·버전 충돌·서버 권한·동시 저장을 확인했습니다. API 검증은 Supabase REST 응답과 조건부 갱신을 모사합니다. 실제 연결 검증 결과는 배포 시 별도로 확인합니다.

설정 참고 문서(2026.9.9. 확인):

- [Vercel Node.js Functions](https://vercel.com/docs/functions/runtimes/node-js)
- [Vercel 프로젝트 설정](https://vercel.com/docs/project-configuration)
- [Supabase 서버 환경변수](https://supabase.com/docs/guides/functions/secrets)

서명란 업데이트 검증: 인쇄 전용 HTML을 PDF로 렌더링하여 A4 가로 1페이지, 작품 6개, 결과표 2개, 위원 5명의 서명란 및 동점 안내 표시를 확인했습니다. 검증용 점수는 배포 파일에 포함하지 않았습니다.
