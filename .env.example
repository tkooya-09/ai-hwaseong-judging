# Vercel does not need database secrets. api/state.js forwards to the deployed Supabase Edge Function.
# Edge Function uses Supabase-provided SUPABASE_URL and SUPABASE_SECRET_KEYS (legacy fallback: SUPABASE_SERVICE_ROLE_KEY).
