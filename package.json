{
  "name": "hwaseong-ai-judging",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.cjs && node scripts/build-edge.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=22"
  }
}
